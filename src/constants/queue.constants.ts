/**
 * Constants for queue names to ensure consistency across the application
 */
export const LANDMARKS_QUEUE = 'landmarks'

/**
 * Job names used in the landmarks queue
 */
export const PROCESS_LANDMARKS_JOB = 'process-landmarks'
